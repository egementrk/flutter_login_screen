import 'package:flutter/material.dart';
import 'package:login_screen/sign_in/signin_body.dart';

class SiginScreen extends StatelessWidget {
  const SiginScreen({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SigninBody();
  }
}
